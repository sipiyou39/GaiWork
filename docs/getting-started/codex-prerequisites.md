# Codex prerequisites

- Install Codex CLI so `codex` is on your PATH.
- Authenticate Codex before running Doudou Code (for example via API key or ChatGPT auth supported by Codex).
- Doudou Code starts the server via `codex app-server` per session.
